{
  "name": "generic-agent-development-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check scripts/orchestrator.mjs && node --check scripts/orchestrator-core.mjs",
    "test": "node --test"
  }
}
